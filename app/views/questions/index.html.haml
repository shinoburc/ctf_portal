%h1 Listing questions

%table
  %tr
    %th Name
    %th Description
    %th Answer
    %th Genre
    %th Default point
    %th Attach filename
    %th Attach file
    %th
    %th
    %th

  - @questions.each do |question|
    %tr
      %td= question.name
      %td= question.description
      %td= question.answer
      %td= question.genre_id
      %td= question.default_point
      %td= question.attach_filename
      %td= question.attach_file
      %td= link_to 'Show', question
      %td= link_to 'Edit', edit_question_path(question)
      %td= link_to 'Destroy', question, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Question', new_question_path
